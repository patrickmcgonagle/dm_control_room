# Data directory

`default_state.json` is the tracked, text-only **Shadows of the Misty Forest** demo used for a fresh install.

The server creates runtime files such as `state.json`, `custom_spells.json`, and `library_config.json`. Those files are intentionally ignored because they can contain campaign/user data, imported material, local paths, or credentials.
